# Evaluation — 2026-09-10

A fresh Pi run with explicit candidate/core skill loading made one Atlas local
search for Austin dental clinics. HTTP200 returned20 rows; the output kept10
branch rows with nine tentative company groups. Two rows used the broader
Dentist category and were marked uncertain. Names, categories, addresses,
websites and phones in the ten rows matched the saved response in an
independent review. This does not independently verify real business data.

Receipt at capture: paid USD0, held USD0.02, pending. No detail purchases,
contact messages or retry occurred. An ambiguous near-me boundary requested
location, category and product before spending.

Review found that the brief omitted the ten capped-out rows from its count
reconciliation. Instructions now include capped-out rows and treat uncertain
rows as a delivered subset. A clean saved-data replay passed: 20 raw = 0 duplicates + 0 excluded +
10 capped out + 10 delivered. The two uncertain rows remained a delivered
subset. Independent review matched all ten contact rows to the saved input.
The replay read only the candidate and two specified input files; it made no
network requests. An earlier replay that read other run files was invalidated
and excluded.

A fresh no-skill baseline used the identical saved provider body with no new
purchases. Both produced ten useful branch rows. The baseline accounted for
the omitted ten rows before the correction; the skill made company-group
evidence and product-fit hypotheses more explicit. This small analysis-only
comparison does not establish a general performance improvement. The guided
correction replay is not a causal benchmark.

Acquisition performance, automatic triggering, all-city coverage, independent
contact accuracy and final settlement remain untested. Private traces are omitted.
